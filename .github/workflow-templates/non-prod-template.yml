name: Template (non-prod)

run-name: "Template (non-prod) — ${{ github.repository }}"

on:
  workflow_dispatch:
  pull_request:
  push:
    branches-ignore:
      - main
      - master

jobs:
  non-prod:
    name: Non-prod pipeline
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder build step
        run: echo "non-prod build for ${{ github.repository }}"