# Deploy configuration for DEV environment
# Every key=value becomes a Maven -D flag.
# The caller workflow can override any value (e.g. org-specific targets).

# ── App-specific ──────────────────────────────────────────────────
rtf.applicationName=my-mule-app
rtf.replicas=1
rtf.cpuReserved=500m
rtf.cpuMax=1000m
rtf.memoryReserved=700Mi
rtf.memoryMax=1000Mi
skipTests=true

# ── Org/env defaults (overridden by caller for each org) ─────────
anypoint.environment=development
anypoint.businessGroupId=gip
rtf.target=gip-rtf-cluster-dev
rtf.provider=MC
connected.app.grantType=client_credentials
mule.env=dev

anypoint.platform.client.id=fdsafdsa
anypoint.platform.client.secret=adfjjjdkhsafjkdsa 
aws.secret.env=us-east-1
nr_logs_partition=log
nr_data_partition=log
nr_data_encoding=True
logLevel=DEBUG
api.layer=Process
aws.region=us-east-1
api.id=37983789ew
rtf.ingressUrl="https://hello.com/*,https://goodbye.com/*"
rtf.enforceDeployingReplicasAcrossNodes=true
nr_url=https://log-api.newrelic.com/log/v1
rtf.jvm.args="-XX:MaxRAMPercentage=50.0"


